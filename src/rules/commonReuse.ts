/* eslint-disable sonarjs/todo-tag */
// TODO: Common Reuse Principle
//
// Зависимость от контекста (boundary) микросервисов использует
// внешнеконтекстные API всех микросервисов этого контекста.
//
// Если внешний потребитель зависит от части сервисов boundary,
// но не от всех — это сигнал, что boundary нарезан неправильно
// и содержит лишние сервисы с точки зрения потребителя.
//
// Алгоритм:
// 1. Для каждой boundary собираем множество её контейнеров
// 2. Находим внешних потребителей — контейнеры вне boundary,
//    имеющие хотя бы одну связь с контейнером внутри boundary
// 3. Для каждого внешнего потребителя проверяем: использует ли он
//    все контейнеры boundary или только часть
// 4. Нарушение — потребитель зависит от N из M контейнеров, где N < M

// eslint-disable-next-line unicorn/require-module-specifiers
export {};
