/* eslint-disable sonarjs/todo-tag */
// TODO: Common Reuse Principle
//
// Контейнеры внутри одной границы должны переиспользоваться вместе.
// Если контейнер X используется контейнером A, но не используется контейнером B
// (при этом A и B в одной границе), это потенциальное нарушение.
//
// Метрика: для каждой границы считаем долю "неиспользуемых" внутренних связей.
// Нарушение — если в границе есть контейнер, с которым не связан ни один
// другой контейнер этой же границы.

// eslint-disable-next-line unicorn/require-module-specifiers
export {};
